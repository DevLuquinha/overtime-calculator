<UserControl x:Class="overtime_calculator.Views.OvertimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="400"
             FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Background="#FAFAFA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Registrar Horas Extras" 
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                           Margin="0,0,0,30"
                           HorizontalAlignment="Center"
                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                <materialDesign:Card Padding="24" UniformCornerRadius="10" Margin="0,0,0,20">
                    <StackPanel>
                        <DatePicker SelectedDate="{Binding RecordDate}"
                                    Margin="0,0,0,20"
                                    materialDesign:HintAssist.Hint="Data"
                                    Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />

                        <materialDesign:TimePicker SelectedTime="{Binding EntryTime}"
                                                   Is24Hours="True"
                                                   Margin="0,0,0,20"
                                                   materialDesign:HintAssist.Hint="Hora de Entrada"
                                                   Style="{StaticResource MaterialDesignFloatingHintTimePicker}" />

                        <materialDesign:TimePicker SelectedTime="{Binding ExitTime}"
                                                   Is24Hours="True"
                                                   Margin="0,0,0,20"
                                                   materialDesign:HintAssist.Hint="Hora de Saída"
                                                   Style="{StaticResource MaterialDesignFloatingHintTimePicker}" />

                        <TextBox Text="{Binding Description}"
                                 Height="100"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 AcceptsReturn="True"
                                 Margin="0,0,0,20"
                                 materialDesign:HintAssist.Hint="Justificativa / Descrição"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}" />

                        <Button Content="REGISTRAR"
                                Command="{Binding SaveCommand}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Height="40"
                                ToolTip="Registrar horas extras"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>